# ── ntfy server ──────────────────────────────────────────────
# Full URL including topic, e.g. https://ntfy.example.com/idrac-alerts
NTFY_URL=https://ntfy.example.com/idrac-alerts

# Bearer token for ntfy authentication
NTFY_TOKEN=tk_your_token_here

# Optional: override ntfy priority for ALL alerts (low, default, high, urgent)
# If unset, priority is derived from iDRAC severity automatically.
# NTFY_PRIORITY=

# Optional: extra comma-separated tags added to every notification
# NTFY_TAGS=homelab,dell

# ── SNMP settings ───────────────────────────────────────────
# Community string configured in iDRAC SNMP trap destination
SNMP_COMMUNITY=public

# Listen address and port (usually left at defaults)
SNMP_LISTEN_ADDRESS=0.0.0.0
SNMP_LISTEN_PORT=162

# ── Display ─────────────────────────────────────────────────
# Friendly name shown in notification titles
IDRAC_LABEL=iDRAC

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
